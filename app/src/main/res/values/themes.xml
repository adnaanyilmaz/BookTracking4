<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BookTracking4" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="Theme.BookTracking4" parent="Base.Theme.BookTracking4" />
    <!-- styles.xml -->
    <style name="RatingBar" parent="Widget.AppCompat.RatingBar">
        <item name="colorControlActivated">@color/buttonColor</item> <!-- Dolu yıldız rengi -->
        <item name="colorControlNormal">@color/gray</item> <!-- Boş yıldız rengi -->
        <item name="colorControlHighlight">#009688</item> <!-- Yıldız vurgusu -->
    </style>

</resources>